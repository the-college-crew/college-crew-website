# College Crew — required environment variables.
# Copy to .env.local and fill in real values (shared out-of-band, never committed).

# Supabase — project settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Service-role key: server-only, bypasses RLS. Never expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# Provider school recognition. College Scorecard is server-only; request a
# free api.data.gov key for production. Brandfetch's client ID is intentionally
# publishable and directly embedded in recognized-school logo image URLs.
COLLEGE_SCORECARD_API_KEY=
NEXT_PUBLIC_BRANDFETCH_CLIENT_ID=

# Stripe — use one consistent mode per deployment. Prefer a least-privilege
# restricted server key after its required permissions are verified in
# Workbench. Production uses live keys; local/preview may remain in test mode.
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
# Optional second signing secret when Accounts v2 thin events use a separate
# live event destination from payment/dispute snapshot events.
STRIPE_CONNECT_WEBHOOK_SECRET=

# Resend — auth and transactional booking email. In local development a
# missing key keeps email testable without delivery. Any deployed hourly
# environment requires the key, a verified EMAIL_FROM identity, and founder
# operations recipients. The verified project domain is send.thecollegecrew.com.
RESEND_API_KEY=
EMAIL_FROM="College Crew <no-reply@send.thecollegecrew.com>"
# Signs delivery/bounce/complaint webhooks sent to /api/webhooks/resend.
RESEND_WEBHOOK_SECRET=

# Hourly Booking scheduler. NEXT_PUBLIC_SITE_URL is the canonical HTTPS origin
# with no path. BOOKING_CRON_SECRET is a 32+ character server-only bearer secret
# shared with Supabase Vault. Founder addresses are comma-separated.
NEXT_PUBLIC_SITE_URL=https://thecollegecrew.com
BOOKING_CRON_SECRET=
FOUNDER_OPERATIONS_EMAILS=

# OpenAI — powers the gpt-5.4-nano scan of provider bios and display names
# (lib/moderation/profile-text.ts). Optional: with it unset, that layer no-ops
# and the regex layer stands alone. Profile moderation is flag-only, so a missing
# key never stops a provider from saving.
#
# NOTE: this is the APP's copy of the key, read from .env.local (and Vercel).
# The moderate-message Edge Function has its OWN OPENAI_API_KEY in the Supabase
# secret store — see below. Setting one does not set the other.
OPENAI_API_KEY=

# AI Help assistant. Server-only and disabled unless explicitly enabled.
# Preview should use a dedicated capped project key; Production may fall back
# to OPENAI_API_KEY when OPENAI_SUPPORT_API_KEY is unset.
OPENAI_SUPPORT_API_KEY=
AI_SUPPORT_ENABLED=false

# Hourly Booking v1 rollout controls. Both are server-side and default to false
# when omitted. BOOKING_REQUESTS_ENABLED can be turned off independently as the
# rollback switch. Existing booking dashboards/actions must remain available.
HOURLY_BOOKING_ENABLED=false
BOOKING_REQUESTS_ENABLED=false

# ── Edge Function secrets (moderate-message) ────────────────────────────────
# These are NOT read from .env.local — they live in the Supabase Edge Function
# secret store and are set with `npx supabase secrets set NAME=value`, then a
# redeploy (`npx supabase functions deploy moderate-message`). Listed here for
# reference only.
#   OPENAI_API_KEY   — enables the gpt-5.4-nano contact-info scan (Layer 2).
#                      Unset → the function no-ops the model pass; regex stands
#                      alone. Never blocks a message on an OpenAI outage.
#   RESEND_API_KEY   — lets the function email founders when a message is
#                      flagged. Unset → it logs the notice instead of sending.
#   EMAIL_FROM       — sender identity for that notification (same verified
#                      Resend domain as above).
