import { EmptyState } from "@/components/ui";

/** Placeholder: full browse (providers by service, distance sort) lands with flow P-3. */
export default function Browse() {
  return (
    <EmptyState
      title="Browse is on the way"
      body="Provider browsing arrives in the next build. Services are listed on the Home tab."
    />
  );
}
