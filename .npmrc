# apps/web (react 19.2) and apps/mobile (react 19.1, pinned by Expo SDK 54)
# legitimately need different React versions; npm's strict peer resolution
# cannot express that across hoisted workspaces, so relax it repo-wide.
legacy-peer-deps=true
