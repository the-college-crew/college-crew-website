@AGENTS.md

# Git Identity (Zach's machine)
The College Crew business project — separate from personal projects.
All remotes must use the `github.com-collegecrew` SSH host alias
(e.g. `git@github.com-collegecrew:the-college-crew/...`), never plain
`github.com`, which would authenticate with the wrong key.
