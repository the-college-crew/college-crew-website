# Run log — Proposer — 2026-08-06

**Routine:** Proposer
**Fired:** 2026-08-06
**Outcome:** One new proposal — CC-010, effort `S`. Track A produced no survivor this run; said so rather than padding the queue.

## Read

- `docs/agents/README.md` (full — unchanged since 2026-08-05)
- `CLAUDE.md`
- `docs/agents/decisions.md`
- `docs/agents/backlog.md`

## Backpressure check

| Item | Status | Effort | Plan |
|---|---|---|---|
| CC-001 | rejected | L | — |
| CC-005 | rejected | S | — |
| CC-008 | approved (PR #216 open, awaiting merge) | S (pure) | none needed |
| CC-009 | approved (PR #217 open, awaiting merge) | S (pure) | none needed |

CC-003, CC-004, CC-006, CC-007 are all merged and moved to `decisions.md`. No
backlog item is `proposed`; no plan is awaiting approval (the only plan file,
`plans/CC-004.md`, belongs to a done item). Proceeded to research.

More unrelated work landed on `main` since 2026-08-05 — an RLS hardening pass
("revoke unneeded anonymous definer access"), a Preview persona Keychain
helper, and a weekly blog cycle (published + a new draft). None of it routed
through the backlog; none of it changes the backpressure check.

## Research

Eight web searches across both tracks.

**Track A:** provider payout-timing transparency, chat presence/typing
indicators as a service-quality signal, local-services referral and
word-of-mouth growth mechanics, a revisit of "satisfaction guarantee"
framing against what the dispute process actually promises.

**Track B:** Open Graph / Twitter Card metadata for Next.js 16's App Router
metadata API, current guidance on required tags and image sizing.

## Candidates generated and killed (5)

1. **Open Graph + Twitter Card metadata on the root layout** (Track B) —
   **survives as CC-010.**
2. **Referral / first-booking incentive program** (Track A) — killed.
   Research is consistent that working referral programs are *dual-sided
   monetary* incentives ("$25 off for both parties"). Committing to a
   specific discount amount is a business decision only Zach can make, and
   the redemption/fraud-prevention plumbing behind it is real infrastructure
   — well past a day, and not something to invent a number for.
3. **Chat typing indicators / presence** (Track A, service-quality angle) —
   killed. Checked `components/chat/` for any existing presence/typing
   state — none. Not a stated requirement anywhere in `CLAUDE.md`, and
   building it (a Realtime presence channel plus UI) is a new feature, not a
   mechanical fix.
4. **Provider payout-timing transparency during onboarding** (Track A) —
   killed. Wanted to reuse the CC-003/CC-006/CC-009 "surface an existing
   constant" pattern, but the payout-delay logic lives in scheduler/DB
   comments (`lib/stripe/connect.ts`, the `held_funds_payout_model`
   migration), not a single named constant — stating a number here without
   one to point at risks shipping something wrong or stale in provider-
   facing copy.
5. **"Satisfaction guarantee" trust badge** (Track A) — killed. The
   substance already exists and is already surfaced — the homepage already
   states "Cancellation, no-show, refund, and billing concerns follow one
   visible review process." An explicit "guarantee" label risks
   overpromising past what the actual policy delivers (case-by-case founder
   review, not a blanket refund) — same fabrication-risk class as the
   insurance-badge idea killed on 2026-08-05.

No Track A candidate survived this run. Per the prompt's own instruction —
say so rather than invent one. Every angle either needed a monetary
commitment outside an agent's authority, was a new feature rather than a
mechanical fix, or risked overstating what the platform actually promises.

## Open PRs checked

`mcp__github__list_pull_requests` (state=open) at time of this run:

- #217 — CC-009: add "Payments secured by Stripe" line next to the pay button
- #216 — CC-008: add baseline HTTP security headers (no CSP yet)

(#172, #173, #169 from prior runs are all merged/closed since 2026-08-05.)

## Action taken

Appended CC-010 to `docs/agents/backlog.md` (status `proposed`) and committed
this run log, both on branch `agents/proposals-2026-08-06`, PR, self-merge
under rule 7 (diff confined to `docs/agents/backlog.md` and
`docs/agents/runs/`). The `## Slack` block below is what the notify-pilot
GitHub Action posts to `#agents` once this merges.

## Slack

```text
*Proposer — 2026-08-06*

*Open PRs awaiting your merge:*
• #217 — CC-009: add "Payments secured by Stripe" line next to the pay button
• #216 — CC-008: add baseline HTTP security headers (no CSP yet)

One new proposal below, `proposed` and effort `S` — approving is one word in backlog.md, and pure-S skips planning, straight to tonight's Worker. <@U0BMD6360GM>

*CC-010 — Add Open Graph and Twitter Card metadata to the root layout* (S)
`app/layout.tsx` already sets metadataBase, a title template, and a description — but zero openGraph or twitter fields exist anywhere in the app. Every link to College Crew (a flyer QR scan, a text to a neighbor, a campus group-chat share) unfurls as a bare URL instead of a title/description/image card on iMessage, WhatsApp, Slack, Discord. next.config.ts's own comments describe the flyer/QR rewrite system as the pilot's actual acquisition mechanism — this is a word-of-mouth pilot, and word of mouth today mostly travels as a pasted link. Add openGraph/twitter to the root metadata export using the existing SITE.name/SITE.description and the existing college-crew-mark.png logo — no new asset, no new copy. Every page inherits it via Next's normal metadata merging.
_Why this one:_ same "one shared surface, every page inherits it" shape as CC-007's FieldError fix — a verified-zero gap in the one file that already holds the site-wide metadata baseline, fixed with data the codebase already has.
_Devil's advocate:_ at pilot scale, few links are actually shared yet, so this may not move a measurable number. Survives anyway — one metadata block, existing brand assets, zero ongoing cost, and much cheaper to add now than to retrofit once dozens of provider/blog pages exist to not inherit it.

*Cut list* (5 candidates generated, 4 killed):
• Referral / first-booking incentive program — real referral programs are dual-sided monetary incentives; committing to a discount amount is Zach's call, and the redemption/fraud plumbing is real infrastructure. Not a day.
• Chat typing indicators / presence — no existing presence state found; a new Realtime feature, not a mechanical fix.
• Provider payout-timing transparency — wanted the same "surface an existing constant" pattern as CC-003/006/009, but no single named constant exists to point at; stating a number without one risks shipping something wrong.
• "Satisfaction guarantee" trust badge — the substance already exists and is already surfaced on the homepage; an explicit "guarantee" label risks overpromising past what the dispute process actually delivers. Same fabrication-risk class as the insurance-badge idea killed 2026-08-05.

*Track A produced no survivor this run* — said so rather than padding with a weak pick.

*Most useful thing research turned up:* the app has metadataBase already configured (the prerequisite Next.js needs to resolve share-image URLs) but nothing built on top of it — the hard part was already done by accident.
```
